server:
  port: 8888

#spring:
#  cloud:
#    config:
#      server:
#        git:
#          uri: https://github.com/spring-cloud-samples/config-repo.git
#          searchPaths: licenseservice,organizationservice
#          username: usernamehere
#          password: passwordhere
#          default-label=main

spring:
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config/